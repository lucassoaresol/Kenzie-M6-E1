import styled from "styled-components";

const StyledErrorsMessage = styled.p`
  display: inline-flex;
  align-items: center;
  justify-content: flex-end;
  font-size: 10px;
  color: var(--Color-primary);
  gap: 2px;
`;

export { StyledErrorsMessage };
